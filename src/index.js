import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(

        <App id="nesto"/>

);

//  [1,2,3,4,5].forEach(e => {
//     var box = document.createElement('DIV');
//     box.innerHTML = e;
//     document.body.appendChild(box);
// })

// class Nesto {
//     constructor() {this.nesto = "Pera"}
//     hello() {console.log(this.nesto)}
// }

// var obj = new Nesto()

// var el = document.createElement("DIV")
// el.innerHTML = "Zika"
// document.body.appendChild(el)
// el.addEventListener("click", obj.hello.bind(obj))

{/* <App></App>
console.log(
    <div></div>
) */}